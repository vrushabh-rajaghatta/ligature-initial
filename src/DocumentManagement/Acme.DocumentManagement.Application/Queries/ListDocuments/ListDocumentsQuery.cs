namespace Acme.DocumentManagement.Application.Queries.ListDocuments;

/// <summary>
/// This deployment's documents. No parameters today — the record exists so
/// the first one is a change to a type, not to a method signature (SC-003).
/// </summary>
public sealed record ListDocumentsQuery;
